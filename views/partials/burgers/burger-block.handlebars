<li>
	{{name}}

	{{#unless devoured}}
	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		devour TIME!
	</button>
	{{/unless}}
	{{#if devoured}}
	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">re-stock!</button>
	{{/if}}
</li>